import Link from "next/link";
import { ArrowLeft, Building2, CalendarClock, CheckCircle2, Clock3, MapPin, Package, Truck } from "lucide-react";

import { PageContainer } from "@/components/layout/page-container";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import type { DestinationOrder, MealPlanSubscription } from "@/types/domain";

export function DestinationOrderDetail({
  order,
  plan,
  onAdvance,
}: {
  order?: DestinationOrder;
  plan?: MealPlanSubscription;
  onAdvance?: () => void;
}) {
  const isPlan = Boolean(plan);
  const destination = order?.destination ?? plan?.destination;
  const destinationLabel = order?.destinationLabel ?? plan?.destinationLabel;
  const window = order?.deliveryWindow ?? plan?.firstDeliveryWindow;
  const status = order?.status ?? plan?.status;
  return <div className="min-h-screen bg-[var(--background)] pb-10"><section className="bg-[var(--charcoal)] py-8 text-white sm:py-10"><PageContainer><Link href="/orders" className="inline-flex min-h-10 items-center gap-2 text-xs font-bold text-white/60 hover:text-white"><ArrowLeft className="size-4" /> Journey log</Link><p className="mt-7 flex items-center gap-2 text-[0.65rem] font-bold uppercase tracking-[0.16em] text-[var(--orange)]">{isPlan ? <Building2 className="size-4" /> : <Truck className="size-4" />}{isPlan ? "Dabba / Tiffin plan" : "Destination delivery"}</p><h1 className="mt-2 font-display text-3xl font-bold tracking-[-0.05em] sm:text-4xl">{isPlan ? plan?.plan.name : order?.meal.name}</h1><p className="mt-2 text-sm text-white/55">A simulated view of what is planned for {destinationLabel}.</p></PageContainer></section><PageContainer className="-mt-4 space-y-5 sm:-mt-6"><section className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-5"><div className="flex items-start gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-[#fff0e8] text-[var(--orange)]"><MapPin className="size-5" /></span><div><p className="text-sm font-bold">{destinationLabel}</p><p className="mt-1 text-xs font-semibold text-[var(--ink-soft)]">{destination?.name} · {destination?.area}</p><p className="mt-1 flex items-center gap-1 text-xs font-semibold text-[var(--green)]"><CalendarClock className="size-3.5" /> {window?.dayLabel} · {window?.label}</p></div></div><div className="mt-5 flex items-center justify-between gap-3 border-t border-[var(--border)] pt-4"><div><p className="text-[0.62rem] font-bold uppercase tracking-[0.1em] text-[var(--ink-faint)]">Status</p><Badge className="mt-1" tone={isPlan ? "amber" : status === "delivered" ? "green" : "orange"}>{isPlan ? "Active simulated plan" : status === "out-for-delivery" ? "Out for delivery" : status}</Badge></div>{order?.orderNumber ?? plan?.subscriptionNumber ? <div className="text-right"><p className="text-[0.62rem] font-bold uppercase tracking-[0.1em] text-[var(--ink-faint)]">Reference</p><p className="mt-1 font-display text-lg font-bold">{order?.orderNumber ?? plan?.subscriptionNumber}</p></div> : null}</div></section>{isPlan ? <section className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-5"><div className="flex items-center gap-2"><Package className="size-5 text-[var(--orange)]" /><h2 className="font-display text-lg font-bold">Plan details</h2></div><dl className="mt-4 grid gap-4 text-sm sm:grid-cols-2"><div><dt className="text-xs font-semibold text-[var(--ink-faint)]">Cadence</dt><dd className="mt-1 font-bold">{plan?.plan.cadenceLabel}</dd></div><div><dt className="text-xs font-semibold text-[var(--ink-faint)]">Duration</dt><dd className="mt-1 font-bold">{plan?.plan.durationWeeks} weeks</dd></div><div><dt className="text-xs font-semibold text-[var(--ink-faint)]">Delivery days</dt><dd className="mt-1 font-bold">{plan?.plan.deliveryDays.join(" · ")}</dd></div><div><dt className="text-xs font-semibold text-[var(--ink-faint)]">Billing</dt><dd className="mt-1 font-bold">Simulated only</dd></div></dl><p className="mt-5 flex items-start gap-2 rounded-xl bg-[#fff3d7] p-3 text-xs font-semibold leading-5 text-[#795910]"><CheckCircle2 className="mt-0.5 size-4 shrink-0" />{plan?.billingLabel}</p></section> : <section className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-5"><div className="flex items-center gap-2"><Clock3 className="size-5 text-[var(--green)]" /><h2 className="font-display text-lg font-bold">Delivery progress</h2></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-[#e8f4ed] p-3"><p className="text-xs font-bold">Confirmed</p><p className="mt-1 text-[0.65rem] font-semibold text-[#246848]">Meal accepted</p></div><div className="rounded-xl bg-[var(--surface-muted)] p-3"><p className="text-xs font-bold">Preparing</p><p className="mt-1 text-[0.65rem] font-semibold text-[var(--ink-soft)]">Kitchen step</p></div><div className="rounded-xl bg-[var(--surface-muted)] p-3"><p className="text-xs font-bold">Delivered</p><p className="mt-1 text-[0.65rem] font-semibold text-[var(--ink-soft)]">At destination</p></div></div>{onAdvance && status !== "delivered" ? <Button variant="outline" className="mt-5" onClick={onAdvance}>Advance simulated status</Button> : null}</section>}</PageContainer></div>;
}
